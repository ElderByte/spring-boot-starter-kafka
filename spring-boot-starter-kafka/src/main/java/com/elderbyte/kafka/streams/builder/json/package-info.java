/**
 * The Kafka Streams Json package contains
 * very opinionated builders:
 *
 * Generally, it is assumed that all Keys are strings
 * and all values are JSON.
 *
 * Composite keys are supported and encoded as single strings
 * using delimiters.
 *
 */
package com.elderbyte.kafka.streams.builder.json;
